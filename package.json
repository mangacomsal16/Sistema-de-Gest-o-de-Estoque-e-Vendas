{
  "name": "estoque-vendas",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de Gestão de Estoque e Vendas — monorepo (client + server)",
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,magenta \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --workspace server run dev",
    "dev:client": "npm --workspace client run dev",
    "db:setup": "npm --workspace server run prisma:migrate && npm --workspace server run prisma:seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
